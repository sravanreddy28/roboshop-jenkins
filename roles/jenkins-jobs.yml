- name: create jenkins jobs
  hosts: localhost
  roles:
    - jenkins
  vars:
    mjobs:
      - { job_name : "cart", repo_name : "https://github.com/sravanreddy28/cart.git"}

    sjobs:
      - { job_name : "roboshop-terraform", repo_name : "https://github.com/sravanreddy28/roboshop-terraform.git" }


